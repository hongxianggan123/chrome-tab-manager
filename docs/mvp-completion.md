# MVP 完成检查

状态：MVP 已完成，下一步进入 MVP 后置功能阶段。

更新时间：2026-06-16

本文档记录当前代码对冻结 MVP 范围的覆盖情况，以及进入下一阶段前需要保留的实现约束。

## 完成结论

MVP 目标已经达成：

1. 用户可以在侧边栏查看所有普通窗口中的标签实例。
2. 用户可以识别规范化 URL 相同的重复标签页。
3. 用户可以跳转、关闭、归档打开中的标签实例。
4. 用户可以查看、恢复和删除归档项。
5. 用户可以用搜索和状态过滤缩小列表。

当前实现可以作为下一阶段开发基线。后续新增能力不应改写 MVP 语义，除非先更新范围和 ADR。

## MVP 覆盖清单

| 范围 | 状态 | 实现说明 |
| --- | --- | --- |
| MV3 扩展骨架 | 完成 | `manifest`、side panel、service worker、Vite 构建已可用。 |
| 只管理普通窗口 | 完成 | snapshot 读取普通窗口并排除隐身窗口。 |
| 标签实例清单 | 完成 | side panel 展示当前打开标签实例，包含标题、窗口标识和状态徽标。 |
| 完整 host 分组 | 完成 | 默认按完整 hostname 分组，分组显示总数、打开、归档、重复计数。 |
| 分组折叠持久化 | 完成 | 用户手动折叠状态写入 `chrome.storage.local`；搜索或过滤时临时展开。 |
| 重复识别 | 完成 | 规范化 URL 保留 origin、path、query，忽略 fragment；重复项不合并行。 |
| 搜索 | 完成 | 搜索标题、hostname、规范化 URL，并覆盖打开项和归档项。 |
| 状态过滤 | 完成 | 支持全部、打开、归档、重复。 |
| 跳转 | 完成 | 激活目标 tab 并聚焦所在 window。 |
| 关闭 | 完成 | 单个关闭不创建归档记录，不提供撤销。 |
| 归档 | 完成 | 普通网页单个归档；特殊 URL 拒绝归档；同 URL 仍有打开实例时不创建归档状态。 |
| 恢复归档 | 完成 | 在当前普通窗口打开归档原始 URL，成功后删除归档状态。 |
| 删除归档记录 | 完成 | 只删除扩展本地归档状态。 |
| active/archived 互斥 | 完成 | 派生 inventory 时打开实例优先，已有打开实例时归档项不进入可见列表。 |
| loading/empty/error | 完成 | 首次加载、空状态、读取失败和操作失败均有界面反馈。 |
| 基础无障碍 | 完成 | 主要行和操作可聚焦，按钮有 `aria-label`，状态不只依赖颜色表达。 |
| 当前页定位 | 完成 | 当前 active tab 高亮，且 active tab 变化时锚定到列表可视区域。 |
| 完整 URL 查看 | 暂缓 | 已从当前 UI 移除；后续单独设计查看入口，避免占用高密度列表空间。 |

## 已确认的 UI 调整

以下是实现过程中根据实际体验调整后的 MVP 交互，后续文档和实现应以此为准：

- 操作按钮不使用 tooltip 作为主要说明，依赖 `aria-label`、图标语义和行内布局。
- 完整 URL 不通过 hover tooltip 展示；底部 URL inspector 已移除，后续单独设计查看入口。
- 行点击作为打开项跳转或归档项恢复的主操作，行内按钮只保留归档、关闭、删除等次要动作。
- favicon 放在分组标题中展示，同 host 使用组级 icon；行内不重复展示 favicon。
- 当前 active tab 需要高亮，并在列表中自动滚动到可视位置。
- 组内打开项采用稳定排序，不使用 `lastAccessed` 排序，避免点击后列表跳变。

## 已验证命令

最近一次完成检查通过：

```bash
npm run typecheck
npm run test
npm run build
```

单元测试覆盖当前核心纯逻辑和 storage schema：

- URL 规范化。
- 特殊 URL。
- 重复计数。
- active/archived 合并。
- host 分组和稳定排序。
- 搜索和状态过滤。
- storage 默认值和 migration。

## 已验证 UI 行为

已用本地 dev 页面验证：

- 窄侧边栏宽度下，当前行高亮可见。
- 点击非当前行后，demo active 状态迁移，当前行自动锚定到可视区域。
- 顶部搜索和过滤区域不随页面整体滚走；滚动发生在标签列表内部。
- 当前 UI 不展示完整 URL，列表以标题和状态元信息为主。

真实 Chrome 扩展仍建议在每次构建后到 `chrome://extensions/` reload unpacked extension，再按 [测试计划](./test-plan.md) 做手动回归。

## MVP 后置 Backlog

以下能力不属于已完成 MVP，进入下一阶段排期：

1. 自定义分组配置，包含完整 host 匹配和规范化 URL 正则规则。
2. 侧边栏 URL 级置顶和置顶区。
3. 新页面重复提示和自动重复处理。
4. 休眠标签页识别、实例级保活和休眠自动归档。
5. 最近自动归档记录。
6. 内存指标按需采集和展示。
7. 独立设置页。
8. 更完整的真实 Chrome 手动回归记录。

## 已完成的 MVP 后置能力

- 批量操作和选择模型：行级选择、分组级选择当前可见项、批量关闭、批量归档、批量删除归档记录，以及执行前确认摘要已完成。

## 下一阶段建议

下一阶段建议从自定义分组配置开始，原因：

- 用户已经明确要求支持完整域名和自定义分组能力。
- 当前 domain 分组逻辑已经稳定，适合扩展规则匹配层。
- 该阶段不需要新增 Chrome 权限，风险低于休眠自动归档和内存指标。

建议的执行顺序：

1. 设计并持久化自定义分组配置 schema。
2. 在 domain 层加入 `groupingRules` 匹配，默认 host 分组作为 fallback。
3. 添加设置入口或轻量配置 UI。
4. 为 host 匹配、URL 正则、规则优先级和无效正则增加测试。
5. 更新测试计划和风险清单。
